# Base URL of the backend API server (no trailing /api — the client appends it).
# Local development:
VITE_API_URL=http://localhost:5000
# Production example:
# VITE_API_URL=https://api.mangofarm.com
